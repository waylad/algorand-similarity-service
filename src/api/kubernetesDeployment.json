{
  "apiVersion": "apps/v1",
  "kind": "Deployment",
  "metadata": { "name": "algorand-similarity-service-api" },
  "spec": {
    "replicas": 3,
    "selector": { "matchLabels": { "app": "algorand-similarity-service-api" } },
    "template": {
      "metadata": { "labels": { "app": "algorand-similarity-service-api" } },
      "spec": {
        "terminationGracePeriodSeconds": 30,
        "containers": [
          {
            "name": "algorand-similarity-service-api",
            "image": "registry.digitalocean.com/neutrino/algorand-similarity-service-api",
            "imagePullPolicy": "Always"
          }
        ]
      }
    }
  }
}
